#!/usr/bin/env bash
# script displays a counter for hours and minutes
H="0"
MIN="1"
while [ $H -le 12 ]; do
    echo "Hour: ${HOUR}"
    while [ $MIN -lt 60 ]; do
	echo $MINUTE
	let MIN+=1
    done
    let MIN=0
    let H+=1
done
